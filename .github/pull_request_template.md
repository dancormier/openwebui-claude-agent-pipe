<!-- claude_agent_pipe.py and docs/valves.md are generated. Edit src/*.py,
     run `python3 build.py`, and commit the outputs with your change; CI
     rejects a stale build. -->

## What

## Why

## Checklist

- [ ] `python3 build.py` run; built file and `docs/valves.md` committed
- [ ] `for t in test_*.py; do python3 "$t"; done` green
- [ ] CHANGELOG entry under Unreleased (if a user would notice)
